defmodule BinbaseBackend.OrdersTest do
    alias BinbaseBackend.Orders
    
    test "order matching" do
        #Orders.create_order(maker_id, token_rel, token_base, kind, price, amount)
    end
end
